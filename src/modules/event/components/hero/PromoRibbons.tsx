type Props = {
  text1: string // dòng hồng phía trên
  text2: string // dòng xám phía dưới
  className?: string // optional: thêm class ngoài cùng
}

const PromoRibbons = () => {
  return <div>PromoRibbons</div>
}

export default PromoRibbons
